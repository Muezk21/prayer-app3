{
  "scripts": {
    "dev:backend": "cd backend && uvicorn main:app --reload --host 0.0.0.0",
    "dev:frontend": "cd frontend && npm run dev -- --host --port 3000",
    "dev": "npm-run-all --parallel dev:backend dev:frontend"
  },
  "devDependencies": {
    "@types/luxon": "^3.7.1",
    "npm-run-all": "^4.1.5"
  }
}
